{
  "name": "osu-replay-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Browser-native osu! replay (.osr) player with frame-accurate scrubbing",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "osu-classes": "^3.1.0",
    "osu-parsers": "^4.1.7"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
